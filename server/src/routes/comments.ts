// import express from 'express';
// import * as commentsController from '../controllers/comments';
// import authMiddleware from '../middlewares/auth';

// const router = express.Router();

// router.post('/', authMiddleware, commentsController.createComment);
// router.get('/blog/:blogId', commentsController.getAllCommentsByBlogId);
// router.get('/forum/:forumId', commentsController.getAllCommentsByForumId);
// router.get('/:id', commentsController.getCommentById);
// router.patch('/:id', authMiddleware, commentsController.updateCommentById);
// router.delete('/:id', commentsController.deleteCommentById);

// export default router;